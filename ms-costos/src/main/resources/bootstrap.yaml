spring:
  application:
    name: ms-costos
    # student-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/db-costos
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
