spring:
  application:
    name: ms-solicitud
    # student-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/db-solicitud
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
